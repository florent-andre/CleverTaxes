
"views": {
       "2011": {
           "map": "function(doc) {\n  if(doc.type=='2011' && doc.graphicName) {\n\temit(doc.id, doc);\n\n  }\n}"
       },
       "AEPLF2014": {
           "map": "function(doc) {\n  if(doc.type=='AEPLF2014' && doc.graphicName) {\n\temit(doc.id, doc);\n\n  }\n}"
       },
       "AELFi2014": {
           "map": "function(doc) {\n  if(doc.type=='AELFi2014' && doc.graphicName) {\n\temit(doc.id, doc);\n\n  }\n}"
       },
       "CPPLF2014": {
           "map": "function(doc) {\n  if(doc.type=='CPPLF2014' && doc.graphicName) {\n\temit(doc.id, doc);\n\n  }\n}"
       },
       "CPLFi2014": {
           "map": "function(doc) {\n  if(doc.type=='CPLFi2014' && doc.graphicName) {\n\temit(doc.id, doc);\n\n  }\n}"
       },
       "AELF2013": {
           "map": "function(doc) {\n  if(doc.type=='AELF2013' && doc.graphicName) {\n\temit(doc.id, doc);\n\n  }\n}"
       },
       "CPLF2013": {
           "map": "function(doc) {\n  if(doc.type=='CPLF2013' && doc.graphicName) {\n\temit(doc.id, doc);\n\n  }\n}"
       }
   }
